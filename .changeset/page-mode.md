---
"stitches": minor
---

Add page mode to stitch view: split patterns into configurable pages with optional fuzzy edges that snap to natural colour changes. Navigate via on-screen arrows, page indicator (tap for grid), or keyboard arrow keys.
