---
"stitches": patch
---

Fix canvas interaction bugs: grid lines fade out smoothly at low zoom (raised thresholds + alpha ramp), move YAML serialization to isolate so auto-save no longer blocks stitch marking, widen double-click window to 500ms, and add backstitch chain mode (Ctrl on desktop, toggle button on touch).
